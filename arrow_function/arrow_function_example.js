// const soma = (a, b) => a + b; // nesse caso de uma linha não precisa do return

// console.log(soma(4, 6));

// arrow function não faz hoisting!

// console.log(soma(2, 4));

// function soma(a, b) {
//     return a + b;
// }

// ERROR

soma(2, 4)

const soma = (a, b) => a + b;
